export function RightPanel() {
  return (
    <aside className="w-[360px] border-l bg-background p-4 hidden lg:block">
      <div className="text-sm text-muted-foreground">
        Select a node to inspect
      </div>
    </aside>
  );
}
